import 'package:get_it/get_it.dart';

final inject = GetIt.instance;

Future<void> initInjection() async {
  // inject.registerFactory(() => VaccineTableBloc());
  // inject.registerLazySingleton(() => inject<DioClient>().dio);
}
